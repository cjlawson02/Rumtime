# LAN device base URL (MSW intercepts in dev)
VITE_DEVICE_API_BASE=http://rumtime.local

# 4-digit setup PIN — required for production builds (see lib/config.ts).
VITE_SETUP_PIN=

# Status poll interval (ms)
VITE_DEVICE_POLL_MS=500
